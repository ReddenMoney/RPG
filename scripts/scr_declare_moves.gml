///Never called, the enum contained within is declared at compile time

enum move {
    useItem,
    //Physical Moves
    slash, bash, punch, shoot, kick,
    //Skills
    counter, bleedEdge,
    wildSwing, crushBlow,
    steal, paraChop,
    analyze, scrapBuild, explodeShot, mechaMimic,
    //Waste Turn
    smile
}
